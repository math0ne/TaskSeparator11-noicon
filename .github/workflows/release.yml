name: Build and Release

on:
  push:
    tags:
      - 'v*.*.*'

jobs:
  build:
    runs-on: windows-latest

    steps:
    - name: Checkout code
      uses: actions/checkout@v4

    - name: Setup .NET
      uses: actions/setup-dotnet@v4
      with:
        dotnet-version: '6.0.x'

    - name: Restore dependencies
      run: dotnet restore TaskSplitter11.sln

    - name: Build solution
      run: dotnet build TaskSplitter11.sln -c Release --no-restore

    - name: Prepare release artifacts
      shell: pwsh
      run: |
        # Create release directory
        New-Item -ItemType Directory -Force -Path release
        New-Item -ItemType Directory -Force -Path release/icons

        # Copy all TaskSplitter11 files (excluding Shortcuts directory if present)
        Get-ChildItem "TaskSplitter11/bin/Release/net6.0-windows" -Exclude Shortcuts,icons | Copy-Item -Destination release/ -Recurse -Force

        # Copy icons from source directories (icons were excluded above)
        Copy-Item "Splitter/icons/separator.ico" -Destination release/icons/ -Force
        Copy-Item "SplitterBlank/icons/dark-gray.ico" -Destination release/icons/ -Force
        Copy-Item "SplitterBlank/icons/ultra-faint.ico" -Destination release/icons/ -Force -ErrorAction SilentlyContinue
        Copy-Item "SplitterBlank/icons/ultra-faint-dark.ico" -Destination release/icons/ -Force -ErrorAction SilentlyContinue

    - name: Create release archive
      shell: pwsh
      run: |
        Compress-Archive -Path release/* -DestinationPath TaskSeparator11-${{ github.ref_name }}.zip

    - name: Create GitHub Release
      uses: softprops/action-gh-release@v1
      with:
        files: TaskSeparator11-${{ github.ref_name }}.zip
        generate_release_notes: true
        draft: false
        prerelease: false
      env:
        GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
